# === Required ===
ADMIN_PASSWORD=replace-with-strong-password
DATABASE_URL=sqlite:///./schedule.db

# === HTTPS (Caddy + Let's Encrypt) ===
# Domain that points (DNS A) to this server IP, e.g. 157.22.186.149
# Example: api.yourdomain.ru
# Leave empty / localhost until you own a domain.
DOMAIN=api.example.com
